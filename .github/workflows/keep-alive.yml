name: Keep API Alive

on:
  schedule:
    # Pings every 10 minutes to prevent Render free tier cold starts
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s ${{ secrets.API_BASE_URL }}/health || true
